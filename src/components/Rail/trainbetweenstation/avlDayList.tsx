export interface avlDayList {
    availablityDate: string; // Date in DD-MM-YYYY format
    availablityStatus: string; // Status of availability, e.g., "RAC 29/RAC 29"
    reasonType: string; // Reason code, e.g., "S"
    availablityType: string; // Availability type, e.g., "2"
    currentBkgFlag: string; // Booking flag, e.g., "N"
    wlType: string; // Waitlist type or code, e.g., "8224"
    _id: string; // Unique identifier, e.g., "675be50b5d55497f20bd12d4
    updatedAt: string; // Unique identifier, e.g., "675be50b5d55497f20bd12d4
  }
  
  